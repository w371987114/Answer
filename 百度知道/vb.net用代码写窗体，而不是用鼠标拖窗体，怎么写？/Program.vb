'此为主函数， 需在项目的启动对象中选择此module（要先取消勾选应用程序框架）
Module mainModule

    Sub Main()

        Application.EnableVisualStyles()
        Application.SetCompatibleTextRenderingDefault(False)
        Application.Run(New Class1)

    End Sub
End Module

